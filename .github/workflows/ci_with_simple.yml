
---

## **🚀 6. `.github/workflows/python-app.yml`** (CI/CD with GitHub Actions)  
This workflow does the following:  
✅ Installs dependencies  
✅ Runs tests  
✅ Stores test results  
✅ Zips and stores the application as an artifact  

```yaml
name: Python CI/CD

on: 
  push:
  pull_request:

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: |
        pytest test_app.py > test-results.txt

    - name: Store test results as an artifact
      uses: actions/upload-artifact@v3
      with:
        name: test-results
        path: test-results.txt

    - name: Build application artifact
      run: |
        zip -r python-app.zip app.py requirements.txt

    - name: Store build artifact
      uses: actions/upload-artifact@v3
      with:
        name: python-app
        path: python-app.zip
